% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeAnnualDegreeDays.R
\name{initCalculation}
\alias{initCalculation}
\title{Initialize Degree Days Calculation}
\usage{
initCalculation(
  fileMapping,
  pop,
  ssp,
  bait,
  tLim,
  hddcddFactor,
  wBAIT = NULL,
  baitPars = NULL
)
}
\arguments{
\item{fileMapping}{A named list containing file paths and metadata. Expected elements include:
\describe{
  \item{tas}{Path to temperature data file.}
  \item{rsds}{Path to solar radiation data file (required if \code{bait} is TRUE).}
  \item{sfcwind}{Path to surface wind data file (required if \code{bait} is TRUE).}
  \item{huss}{Path to humidity data file (required if \code{bait} is TRUE).}
  \item{start}{Start year of the time period.}
  \item{end}{End year of the time period.}
  \item{rcp}{RCP scenario identifier.}
  \item{gcm}{GCM model identifier.}
}}

\item{pop}{SpatRaster with annual population data.}

\item{ssp}{Shared Socioeconomic Pathway scenario.}

\item{bait}{Logical indicating whether to use raw temperature or BAIT as ambient temperature.}

\item{tLim}{Temperature limits for degree day calculations.}

\item{hddcddFactor}{data.frame containing pre-computed degree days.}

\item{wBAIT}{(Optional) Weights for BAIT adjustments. Default is \code{NULL}.}

\item{baitPars}{(Optional) Parameters for BAIT calculations. Default is \code{NULL}.}

\item{countries}{SpatRaster containing the region masks.}
}
\value{
A data frame containing annual degree days.
}
\description{
Initiates the calculation of degree days for a single scenario, model, and time period.
The calculation is split into yearly periods to improve computational stability.
}
\author{
Hagen Tockhorn
}
